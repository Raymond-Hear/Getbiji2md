# Get 笔记下载器

一个面向 `Get 笔记` 的网页工具，用来筛选、搜索并导出笔记内容。

当前版本已经不再按浏览器插件的交互来设计，而是改成了独立网页工作台：

- 首次使用时在“设置”中填写 `API Key` 和 `Client ID`
- 页面会自动加载知识库和笔记列表
- 支持关键词搜索
- 支持按知识库和标签筛选
- 支持单篇下载
- 支持多选后打包下载为 `zip`

## 当前结构

- `index.html`：网页界面
- `popup.js`：页面逻辑、接口请求、筛选与下载
- `icons/`：项目图标资源

## 说明

这个项目当前以“网页版本”为主继续推进，所以插件相关文件没有作为本次主要提交内容。

如果后续要真正部署成线上网站，还需要确认：

- `Get 笔记` 接口是否允许浏览器直接跨域访问
- 如果接口不开放 `CORS`，需要补一个后端代理层

## 本地运行

直接在浏览器打开 `index.html` 即可查看页面结构。

如果要让真实接口在网页环境中可用，请先确认接口跨域策略是否允许前端直连。
